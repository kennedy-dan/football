import React from "react";

const Table = ({ data }) => {
  // const col = Object.keys(data).map(d => d.position)
  console.log(data);
  return (
    <div>
    
    </div>
  );
};

export default Table;
